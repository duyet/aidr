/** Public filename for the extension zip used in release workflow. */
export const AIDR_ZIP_FILENAME = "aidr.zip";

/** Top-level folder inside the zip for Load unpacked. */
export const AIDR_UNPACKED_DIR = "aidr";

/** Chrome cannot open chrome:// from HTTPS; still emit this href. */
export const CHROME_EXTENSIONS_HREF = "chrome://extensions";
