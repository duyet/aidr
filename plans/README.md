# Implementation Plans

Generated by the improve skill on 2026-09-04 against `b4168a5`.
Execute in order unless dependencies say otherwise.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Green CI by excluding dither-kit from Biome format | P1 | S | — | DONE |
| 002  | Grant Clerk admin only via allowlist / publicMetadata | P1 | S | — | TODO |
| 003  | Persist LLM fields on items UPSERT conflict | P1 | S | — | TODO |
| 004  | CWS root-manifest zip + listing assets | P2 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- 001 unblocks seeing test/type CI on master (lint currently fails first).
- 002 and 003 are independent of 001.
- 004 is the remaining Chrome Web Store first-submit work after `b4168a5` (privacy URL, version API, unpacked banner already shipped).

## Findings considered and rejected

- Dual-write ClickHouse now: ALGORITHM.md says D1 is the sole primary store; no CH code remains. Spike later if `/data` outgrows D1.
- Self-hosted CRX `update_url` for consumer Chrome: blocked on Windows/macOS without enterprise policy. CWS is the auto-update path.
- Splitting `NewsIngestWorkflow` (~1420 lines): high risk without characterization tests of step order / D1 `batch()`. Defer.
- Replacing `next-themes` / pinning every `^1.0.0` TanStack range: low leverage vs CI and admin auth.
